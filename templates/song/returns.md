# <Song title> — return tracks

Count the returns against the session before trusting this file. Returns get added and
renamed mid-session, and a stale list survives a long time because nothing contradicts it.

Read them with the `return_track.py` handler from `abletonosc-ext/`. Stock AbletonOSC
cannot see return tracks at all.

Captured: <date>

| Return | Name | Plugin | Role |
|---|---|---|---|
| A | | | |
| B | | | |

## Send architecture

<Which tracks feed which returns, and what the routing is doing musically.>
